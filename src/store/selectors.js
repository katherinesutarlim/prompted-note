export const getPrompt = state => state.prompt;

export const getTextContent = state => state.textContent;

export const getAlert = state => state.alert;

export const getDisplayAlert = state => state.displayAlert;